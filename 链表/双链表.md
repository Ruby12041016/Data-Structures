1. 双链表每个节点增加一个prior指针指向其前驱节点，从而支持双向遍历。插入操作需修改四个指针：s->next、q->prior（若有）、s->prior和p->next，要注意语句顺序防止指针错乱。

2. 双链表插入时若p为尾节点，则其next为null，此时不能修改其后继节点的prior指针，要加if判断是否存在后继节点增强代码健壮性。删除操作同理。

3. 双链表的前插操作可转化为先找到给定节点的前驱，再对前驱执行后插操作；按位插入则是先找到第i-1个节点再执行后插。删除操作通过释放q节点完成，并调整前后指针连接。