1. 循环双链表的尾节点next指向头节点，头节点prior指向尾节点，形成两个方向的闭环。空表时头节点的next和prior均指向自身，判空条件为l->next == l

2. 优点：可以从任意节点开始向前或向后遍历，删除节点时不需要知道前驱节点（通过 prev指针直接访问），环形结构在某些场景下更自然（如轮转调度、约瑟夫环问题）

3. 缺点：每个节点需要额外存储一个指针，空间开销较大，操作逻辑比单链表复杂，需要特别注意循环终止条件，避免无限循环